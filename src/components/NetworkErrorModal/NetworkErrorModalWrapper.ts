import styled from "styled-components";

const NetworkErrorModalWrapper = styled.div`
  margin-top: 1rem;

  .modal__text {
    font-size: 1rem;
    font-weight: 400;
    line-height: 1.5;
    margin-bottom: 1rem;
  }

  .modal__video {
    margin-top: 1rem;
    width: 100%;
    height: 300px;
    border-radius: 0.5rem;
    overflow: hidden;
  }
`;

export default NetworkErrorModalWrapper;
